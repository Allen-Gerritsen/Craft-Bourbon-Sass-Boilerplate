test:
  override:
    - echo "no tests"

dependencies:
  override:
    - npm install
    - bower install
    
compile:
  override:
    - npm run buildjs

deployment:
  staging:
    branch: master
    commands:
      - ./util deploy
